# Install the CUDA-enabled PyTorch build for the target platform separately.
# Install a compatible Triton distribution when using environment_check.py.
pytest==9.1.1
ruff==0.16.4
psutil==7.2.2

# Required only by environment_check.py --check-extension.
ninja==1.13.0
