# Cross-platform project tools. Install a CUDA-enabled PyTorch build and the
# Triton package supported by the target platform before using this file.

# Optional CUDA/C++ extension build helpers.
cmake==4.4.2
ninja==1.13.0
pybind11==3.1.0

# Tests, benchmarks, formatting and hardware monitoring.
pytest==9.1.1
pytest-benchmark==5.3.0
ruff==0.16.4
psutil==7.2.2
nvidia-ml-py==13.610.43

# Result analysis and plotting.
numpy==2.5.2
pandas==3.0.5
matplotlib==3.11.1
